import styled from 'react-emotion'
import {space, width, color, fontSize, fontWeight} from 'styled-system'

const U = styled('u')(space, width, color, fontSize, fontWeight, {})

U.defaultProps = {}

export default U
