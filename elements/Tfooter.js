import styled from 'react-emotion'
import {space, width, fontSize, color} from 'styled-system'

const Tfooter = styled('tbody')(space, width, fontSize, color,  {

})

Tfooter.defaultProps = {
  w: 1,
}

export default Tfooter
