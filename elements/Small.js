import styled from 'react-emotion'
import {space, width, fontSize, color} from 'styled-system'

const Small = styled('small')(space, width, fontSize, color, {})

Small.defaultProps = {}

export default Small
