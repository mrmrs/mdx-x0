import styled from 'react-emotion'
import {space, width, fontSize, color} from 'styled-system'

const Address = styled('address')(space, width, fontSize, color, {})

Address.defaultProps = {}

export default Address
